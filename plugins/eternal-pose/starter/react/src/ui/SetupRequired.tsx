export function SetupRequired() {
  return (
    <main>
      <h1>Trip setup required</h1>
      <p>Add real trip content before using this site.</p>
      <p>Configure a supported map provider before treating map features as available.</p>
    </main>
  );
}
