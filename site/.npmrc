; site/ is its own npm project root, so the repo-root .npmrc does not apply
; here. Pin the public registry so a machine-level mirror (~/.npmrc) can never
; poison this lockfile again — CI and Cloudflare Pages cannot reach a private
; mirror.
registry=https://registry.npmjs.org/
